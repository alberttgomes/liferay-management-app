dependencies {
	api project(":modules:management-service-build:management-service-build-api")

	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
}

buildService {
	apiDir = "../management-service-build-api/src/main/java"
}

group = "management.service.build"